image: nubika/sfdxplus-delta:latest

pipelines:
  pull-requests:
    '**': #this runs as default for any branch not elsewhere defined
      - step:
          name: Show SFDX Version
          runs-on:
            - self.hosted
          script:
            - sfdx update
            - sfdx --version
            - sfdx plugins --core
      - step:
          name: Validate
          runs-on:
            - self.hosted
          script:
            - '[[ "${BITBUCKET_PR_DESTINATION_BRANCH}" == "main" ]] && SFDX_AUTH_URL=$PRO_SFDX_AUTH_URL'
            - '[[ "${BITBUCKET_PR_DESTINATION_BRANCH}" == "uat" ]] && SFDX_AUTH_URL=$UAT_SFDX_AUTH_URL'
            - '[[ "${BITBUCKET_PR_DESTINATION_BRANCH}" == "dev" ]] && SFDX_AUTH_URL=$DEV_SFDX_AUTH_URL'
            - if [ -z "$SFDX_AUTH_URL" ]; then echo "No SFDX_AUTH_URL defined for branch ${BITBUCKET_PR_DESTINATION_BRANCH}"; exit; fi
            - echo $SFDX_AUTH_URL > sfdx_auth_url.txt
            - sfdx force:auth:sfdxurl:store --sfdxurlfile sfdx_auth_url.txt --setalias $SF_USERNAME --json
            - sfdx force:config:set defaultusername=$SF_USERNAME
            - sfdx config:get defaultusername
            - LAST_SUCCESSFUL_COMMIT=$(cat ci-config/last_successful_commit.txt)
            - sfdx sgd:source:delta --to $BITBUCKET_COMMIT --from $LAST_SUCCESSFUL_COMMIT --output .
            - LAST_PACKAGE_XML=$(cat package/package.xml)
            - echo $LAST_PACKAGE_XML
            - tests=$(cat ci-config/specified_tests.txt | tr '\n' ',' | sed 's/\(.*\),/\1 /')
            - echo $tests
            - '[[ -z "$tests" ]] && sf project deploy validate -x package/package.xml --test-level RunLocalTests || sf project deploy validate -x package/package.xml  --test-level RunSpecifiedTests --tests $tests'
  branches:
    dev:
      - step:
          name: Show SFDX Version
          runs-on:
            - self.hosted
          script:
            - sfdx update
            - sfdx --version
            - sfdx plugins --core
      - step:
          name: Deploy to Test
          runs-on:
            - self.hosted
          deployment: Test
          script:
            - echo $PRO_SFDX_AUTH_URL > sfdx_auth_url.txt
            - sfdx force:auth:sfdxurl:store --sfdxurlfile sfdx_auth_url.txt --setalias $SF_USERNAME --json
            - sfdx force:config:set defaultusername=$SF_USERNAME
            - sfdx config:get defaultusername
            - LAST_SUCCESSFUL_COMMIT=$(cat ci-config/last_successful_commit.txt)
            - sfdx sgd:source:delta --to $BITBUCKET_COMMIT --from $LAST_SUCCESSFUL_COMMIT --output .
            - LAST_PACKAGE_XML=$(cat package/package.xml)
            - echo $LAST_PACKAGE_XML
            - tests=$(cat ci-config/specified_tests.txt | tr '\n' ',' | sed 's/\(.*\),/\1 /')
            - '[[ -z "$tests" ]] && sf project deploy start -x package/package.xml --test-level NoTestRun || sf project deploy start -x package/package.xml --test-level RunSpecifiedTests --tests $tests'
            - echo $BITBUCKET_COMMIT > ci-config/last_successful_commit.txt
            - echo $LAST_PACKAGE_XML > ci-config/last_package.xml
            - git add ci-config/last_successful_commit.txt
            - git add ci-config/last_package.xml
            - git commit -m '[skip ci]'
            - git push origin ${BITBUCKET_BRANCH}
    uat:
      - step:
          name: Show SFDX Version
          runs-on:
            - self.hosted
          script:
            - sfdx update
            - sfdx --version
            - sfdx plugins --core
      - step:
          name: Deploy to Staging
          runs-on:
            - self.hosted
          deployment: Staging
          script:
            - echo $PRO_SFDX_AUTH_URL > sfdx_auth_url.txt
            - sfdx force:auth:sfdxurl:store --sfdxurlfile sfdx_auth_url.txt --setalias $SF_USERNAME --json
            - sfdx force:config:set defaultusername=$SF_USERNAME
            - sfdx config:get defaultusername
            - LAST_SUCCESSFUL_COMMIT=$(cat ci-config/last_successful_commit.txt)
            - sfdx sgd:source:delta --to $BITBUCKET_COMMIT --from $LAST_SUCCESSFUL_COMMIT --output .
            - LAST_PACKAGE_XML=$(cat package/package.xml)
            - echo $LAST_PACKAGE_XML
            - tests=$(cat ci-config/specified_tests.txt | tr '\n' ',' | sed 's/\(.*\),/\1 /')
            - '[[ -z "$tests" ]] && sf project deploy start -x package/package.xml --test-level RunLocalTests || sf project deploy start -x package/package.xml --test-level RunSpecifiedTests --tests $tests'
            - echo $BITBUCKET_COMMIT > ci-config/last_successful_commit.txt
            - echo $LAST_PACKAGE_XML > ci-config/last_package.xml
            - git add ci-config/last_successful_commit.txt
            - git add ci-config/last_package.xml
            - git commit -m '[skip ci]'
            - git push origin ${BITBUCKET_BRANCH}
    main:
      - step:
          name: Show SFDX Version
          runs-on:
            - self.hosted
          script:
            - sfdx update
            - sfdx --version
            - sfdx plugins --core
      
      - step:
          name: Deploy to Production
          runs-on:
            - self.hosted
          deployment: Production
          script:
            - echo $PRO_SFDX_AUTH_URL > sfdx_auth_url.txt
            - sfdx force:auth:sfdxurl:store --sfdxurlfile sfdx_auth_url.txt --setalias $SF_USERNAME --json
            - sfdx force:config:set defaultusername=$SF_USERNAME
            - sfdx config:get defaultusername
            - LAST_SUCCESSFUL_COMMIT=$(cat ci-config/last_successful_commit.txt)
            - sfdx sgd:source:delta --to $BITBUCKET_COMMIT --from $LAST_SUCCESSFUL_COMMIT --output .
            - LAST_PACKAGE_XML=$(cat package/package.xml)
            - echo $LAST_PACKAGE_XML
            - tests=$(cat ci-config/specified_tests.txt | tr '\n' ',' | sed 's/\(.*\),/\1 /')
            - '[[ -z "$tests" ]] && sf project deploy start -x package/package.xml --test-level RunLocalTests || sf project deploy start -x package/package.xml --test-level RunSpecifiedTests --tests $tests'
            - echo $BITBUCKET_COMMIT > ci-config/last_successful_commit.txt
            - echo $LAST_PACKAGE_XML > ci-config/last_package.xml
            - git add ci-config/last_successful_commit.txt
            - git add ci-config/last_package.xml
            - git commit -m '[skip ci]'
            - git push origin ${BITBUCKET_BRANCH}

